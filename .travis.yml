language: python
python:
  - '3.5' 
  - '3.6' 
  - '3.7' 
  - '3.8' 
install:
      - pip install -r requirements.txt   # 安装依赖
# script: pytest testcases && pylint fast_tornado testcases
script: python -c "from fast_tornado import VERSION; print(VERSION)"
